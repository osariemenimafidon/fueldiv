# FUELDIV — verification checklist

Generated by `scripts/06_docs.py`. Do not edit by hand; rerun the script.

Register: **10 properties**, **24 overlap rows**, **0 values requiring confirmation**.

Nothing in this project is published until every box below is ticked by the investigator personally. `scripts/07_publish_gate.py` refuses to release while any `[VERIFY]` tag remains in the tree.

---

## V1 — Commercial standard values (paywalled)

Each value below was entered into `scripts/02_register.py` from a consensus standard that is not freely available. Open the standard, confirm the number and the clause, then set `verified=True` on that `Envelope` and replace the `[VERIFY]` citation with the real clause reference.

**These include the EN 590 and EN 15940 density ranges already asserted in FACET's `PROGRAM.md`, which currently carry no citation in the programme repository.**


---

## V2 — API gravity to density conversion

- [ ] The certification envelope is stated in API gravity 32-37 degAPI. Density is derived as rho = 999.016 * 141.5 / (degAPI + 131.5), giving 838.9-864.6 kg/m3. Confirm the conversion, and that the 60 degF reference temperature convention is the right one to compare against commercial specs quoted at 15 degC.

## V3 — The either/or reading of 40 CFR 1090.305(c)

- [ ] The register treats cetane index >= 40 and aromatics <= 35 vol% as ALTERNATIVE compliance paths, not joint requirements. If they are in fact joint, the market row for cetane_number and aromatics both change. Read 1090.305(c) and rule.

## V4 — Federal measurement of in-service diesel properties

- [ ] The register records federal_public_measurement = False for all 10 properties. This rests on the reading that EPA's national fuels survey programme (40 CFR 1090 subpart O) is a gasoline programme and that EPA publishes gasoline fuel quality properties with no diesel equivalent. Confirm against 1090.1410 and 1090.1415 and EPA's fuel quality pages. If diesel IS sampled but unpublished, the gap class for those rows becomes sampled_unpublished, which is a materially different finding.

## V5 — Five transcription spot checks

- [ ] Choose five rows of divergence_register.csv. For each, open the cited CFR provision in a browser and confirm the number in the register cell matches the regulation. Record which five, and the outcome, below.

---

## Reproduction

- [ ] `python3 src/fueldiv/properties.py` — all doctests pass
- [ ] `python3 scripts/02_register.py` — reproduces 10 properties and 1 disjoint envelope(s)
- [ ] Every figure regenerates from `data/processed/stats.json`

---

## Attestation

When every box above is ticked, and only then:

```bash
python3 scripts/07_publish_gate.py --sign
```

Signing asserts that you personally reproduced the pipeline and spot-checked its outputs against the primary sources. It is your name on the deposit.

